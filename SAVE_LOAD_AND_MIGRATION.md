# SAVE_LOAD_AND_MIGRATION

## 현재 상태 점검 결과
- 기존 문서셋(`00_project_overview`, `ARCHITECTURE_MAP`, `CORE_SYSTEMS`, `GAME_LOOP_FLOW`, `UI_LOGIC_MAP`, `PROGRESSION_MODEL`)은 **구조/루프/시스템 개요 중심**으로 잘 정리되어 있다.
- 다만 개발자 문서화 계획서에서 별도 장으로 정의한 `save/load/migration` 관점은 독립 문서가 없어서, 저장 호환성 이슈를 빠르게 추적하기 어렵다.

## Save 흐름 요약 (`main.js`)
1. `autoSave()`가 60초 주기로 `save()`를 호출한다(옵션/오프라인 상태 조건부).
2. `save()`는 먼저 런타임 객체(`autoBattle`, `u2Mutations`)를 동기화한 뒤 `game`을 직렬화한다.
3. 저장 데이터에서 정적 정의/툴팁/코스트 등 재생성 가능한 필드를 대량 제거해 저장 크기를 줄인다.
4. `playerSpire.save()` 결과를 붙인 후 `LZString.compressToBase64`로 압축한다.
5. 기본 저장소는 `localStorage.trimpSave1`이며, 조건 충족 시 PlayFab 클라우드 저장을 추가로 시도한다.

### Save 시 삭제되는 데이터의 의도
- `tooltip`, `cost`, `description`, `title`류: `config.js`에서 재구성 가능.
- `badGuys`, `worldUnlocks`, `mapConfig` 등: 런타임/정의 기반으로 재생성 가능.
- 효과:
  - 저장 용량 감소
  - 버전 간 정적 데이터 변경 시 충돌 완화
  - 단, 삭제 대상 변경은 과거 저장 호환성에 직접 영향

## Load 흐름 요약 (`main.js`)
1. 입력 소스 결정: import 문자열 또는 `localStorage`.
2. Base64 압축 해제 + JSON 파싱 실패 시 사용자에게 공지 후 중단.
3. 버전 가드:
   - 베타 저장을 라이브 클라이언트에 로드 차단.
   - 저장 버전이 실행 중 버전보다 최신이면 로드 차단.
   - `killSavesBelow`보다 낮은 저장은 강제 차단.
4. `resetGame()`으로 런 상태/DOM을 초기화한 뒤, 저장값을 카테고리 단위로 병합한다.
5. 카테고리별 예외 처리(`options`, `equipment`, `heirlooms`, 일부 필드 skip)를 적용한다.
6. 이후 버전별 호환 패치(legacy compatibility blocks)를 순차 실행한다.

## Migration/Compatibility 관찰 포인트
- 마이그레이션은 전용 버전 테이블이 아니라, `load()` 내부의 다수 `if (oldVersion <= x)` 블록으로 누적 관리된다.
- 장점: 즉시 대응이 빠름.
- 리스크:
  - 단일 함수 비대화로 가독성 저하
  - 특정 버전 분기 순서 의존이 강함
  - 테스트 없이 수정 시 회귀 위험 증가

## resetGame의 역할 (`updates.js`)
- `resetGame()`은 단순 상태 초기화가 아니라, UI DOM 초기화까지 대규모로 수행한다.
- 의미:
  - 로드 전 환경을 일관 상태로 정리해 잔존 상태를 제거
  - 대신 Save/Load와 UI 결합도가 높아져 변경 영향 범위가 커짐

## 저장 스키마 키 사전 (운영 관점 요약)

| 스키마 경로 | 저장 시 처리 | 로드 시 처리 | 마이그레이션 민감도 | 메모 |
|---|---|---|---|---|
| `game.global.version/stringVersion/betaV` | 현재 실행 버전으로 재기록 | 버전 가드 후 강제 동기화 | 높음 | 버전 비교의 기준점 |
| `game.global.messages` | 저장 포함 | 구버전(`oldVersion < 3.51`)은 구조 변환 후 반영 | 중간 | 메시지 필터 구조 변경 이력 존재 |
| `game.global.buildingsQueue` | 저장 포함 | 로드 시 `addQueueItem`으로 재구성 | 중간 | 큐 ID 재계산 포함 |
| `game.resources.*` | 동적값 저장 | 카테고리 루프 로드 + 구버전 수치 보정 | 높음 | `max`, `potency`, 평균값 구조 패치 이력 다수 |
| `game.jobs.*` | 저장 포함 | 카테고리 루프 로드 + 개별 버전 보정 | 중간 | `modifier`, `owned` 관련 패치 반복 |
| `game.buildings.*` | 정적 필드 일부 제거 | 동적 필드 중심으로 병합 | 높음 | 증설 공식(`increase.by`) 잦은 수정 |
| `game.portal.*` | 정적 메타 일부 제거 | 로드 후 해금/환급/보정 로직 실행 | 높음 | 밸런스 패치 시 영향 큼 |
| `game.options.menu.*` | 설명/타이틀 등 UI 메타 제거 | enable 값/새 설정 추가 로직 적용 | 중간 | 타입(bool→0/1) 변환 이력 존재 |
| `game.playerSpire` | `playerSpire.save()` 별도 부착 | `playerSpire.load()`로 복원 | 중간 | 본체 스키마와 독립된 하위 저장 블록 |

> 유지 원칙: “정적 정의는 제거하고 동적 상태만 저장” 정책을 유지하되, 제거/복원 규칙이 바뀌면 구버전 import 테스트를 반드시 동반한다.

## 버전 호환 블록 인덱스 (`main.js load()`)

아래 표는 `oldVersion` 기반 분기를 **목적별로 압축 정리한 인덱스**다. (전체 목록이 아닌 운영용 상위 인덱스)

| 버전 구간 | 대표 분기 예시 | 패치 성격 | 점검 포인트 |
|---|---|---|---|
| `<= 1.07` | `1.0`, `1.01`, `<=1.02`, `<=1.06`, `<=1.07` | 초기 데이터 타입/기초 스탯 보정 | 자원 `max` 타입, 주거/웜홀 계산 일관성 |
| `< 2.3` | `<1.1`, `<2`, `<2.213`, `<2.3` | 포탈/진행/옵션 타입 이행 | 특정 월드(59/60) 분기, 옵션 bool→정수 전환 |
| `< 3.3` | `<2.7`~`<3.3` | 통계 재계산, 업적/자동화 플래그 확장 | `stats` 누적치, `autoPrestiges` 타입 안정성 |
| `< 4.0` | `<3.6`~`<4` | 업적 배열 재배치, 마스터리 리워크 이행 | 배열 인덱스 이동 오류, 에센스 환급 중복 |
| `< 4.6` | `<4.01`~`<4.603` | 챌린지/설정 확장, 메시지 토큰 증설 | 신규 설정 기본값, 구세이브 메시지 누락 |
| `< 4.9` | `<4.7`~`<4.9` | 프리셋 구조, 업적/재화 환급, 스파이어 보상 | `mapPresets` 구조, `lootAvgs` 변환, 환급 중복 |

### 인덱스 운영 규칙
1. 새 `if (oldVersion < x)` 블록을 추가하면 위 표에도 행(또는 기존 행의 버전 범위)을 함께 갱신한다.
2. 패치가 **스키마 변경**인지 **밸런스 보정**인지를 명시해, 테스트 우선순위를 나눈다.
3. 한 버전에서 다수 분기를 건드릴 경우 “대표 분기 예시” 칼럼에 핵심 1~2개만 남기고 세부는 코드 코멘트로 위임한다.

## Save/Load 회귀 체크리스트 (수동 검증)

### A. 기본 저장/로드
- [ ] 신규 세션에서 `save()` 후 페이지 리로드 시 핵심 자원/월드/업그레이드가 유지된다.
- [ ] `autoSave()` 주기 동작 중에도 수동 저장/로드 충돌이 발생하지 않는다.

### B. Import/Export 경로
- [ ] Export 문자열을 공백/개행 제거 후 import 했을 때 정상 복원된다.
- [ ] 손상된 문자열 import 시 공지 메시지 후 안전 중단된다.

### C. 버전 가드
- [ ] 더 최신 버전 저장 import 시 차단 메시지가 표시된다.
- [ ] `killSavesBelow` 미만 저장은 로드되지 않는다.
- [ ] 베타↔라이브 교차 import 차단이 의도대로 동작한다.

### D. 마이그레이션 후 상태 확인
- [ ] `game.options.menu` 신규 설정이 누락되지 않는다.
- [ ] `mapPresets`, `lootAvgs`, `stats` 등 구조 변경 필드가 정상 값으로 변환된다.
- [ ] 포탈/마스터리 환급류 패치가 중복 적용되지 않는다.

### E. resetGame 연계 확인
- [ ] 로드 직후 DOM 잔상(버튼 상태/탭 노출/텍스트)이 남지 않는다.
- [ ] UI 초기화 후 상태 반영 순서가 깨져 NaN/undefined 표시가 발생하지 않는다.

## 이번 턴에서 진행한 내용
- 기존 권장 후속 항목이었던 **저장 스키마 키 사전**, **버전 호환 블록 인덱스**, **회귀 체크리스트**를 본 문서에 반영했다.
