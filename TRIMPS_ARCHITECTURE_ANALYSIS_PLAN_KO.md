# Trimps 소스코드 아키텍처 분석 계획 (한국어)

## 목표
Trimps 프로젝트의 전체 아키텍처를 **구조/핵심 시스템/게임 루프/UI/성장 시스템** 관점에서 완전히 이해하고,
추가 개발/리팩터링/밸런싱 작업 시 바로 활용 가능한 분석 문서를 만드는 것을 목표로 한다.

---

## 0) 준비 단계 (분석 환경 정리)

### 수행 내용
1. 코드 로딩 순서와 전역 의존성을 먼저 파악한다.
2. 분석 산출물 템플릿(표/다이어그램/체크리스트)을 미리 만든다.
3. 1차 읽기(스캔)와 2차 읽기(심층) 범위를 분리한다.

### 먼저 볼 파일
- `index.html`
- `README.md`
- `config.js`
- `objects.js`
- `main.js`
- `updates.js`
- `playerSpire.js`

### 추출할 정보
- 스크립트 로드 순서와 전역 객체 생성 순서
- 런타임 진입점(초기화 시작 함수)
- 데이터 정의 파일과 실행 로직 파일의 경계

### 문서화 방식
- `00_project_overview.md`: 파일 역할 요약
- `analysis_log.md`: 읽은 파일/핵심 발견/미해결 질문 기록

---

## 1) 프로젝트 구조 분석 방법

### Step-by-step
1. `index.html`에서 `<script>` 로딩 순서를 기록한다.
2. 로딩 순서를 기준으로 전역 심볼(`game`, 유틸 함수, UI 업데이트 함수) 생성 타이밍을 정리한다.
3. 파일별 책임을 1줄로 고정 정의한다.
4. 기능 축(경제, 전투, 맵, 포탈, 오프라인, UI, 저장/불러오기)으로 다시 재분류한다.

### 우선 분석 파일
1. `index.html`
2. `config.js`
3. `main.js`
4. `objects.js`
5. `updates.js`
6. `playerSpire.js`

### 파일별 추출 정보
- `index.html`
  - UI 영역(자원/전투/맵/포탈/설정) DOM 앵커 ID
  - 인라인 이벤트 핸들러 여부
  - JS 번들 없이 스크립트 직결 구조인지 여부
- `config.js`
  - `newGame()` 기준 상태 스키마
  - `game.global` 핵심 플래그/카운터/모드 값
  - 리소스/업그레이드/건물/직업/맵 관련 기본 데이터
- `main.js`
  - 전체 실행 플로우 제어 함수
  - 시스템 간 호출 허브(전투↔진행↔UI 갱신)
- `objects.js`
  - 보조 시스템(튜토리얼, 이벤트성 오브젝트) 동작
- `updates.js`
  - 버전 마이그레이션 로직과 저장 호환성 규칙
- `playerSpire.js`
  - 스파이어(특수 콘텐츠) 독립 서브시스템 구조

### 문서화 방식
- `ARCHITECTURE_MAP.md`
  - 파일 역할 맵
  - 시스템 경계도
  - 의존 방향(누가 누구를 호출하는지)

---

## 2) 핵심 시스템 식별 방법

### Step-by-step
1. `main.js`에서 중심 함수군(예: `battle`, `nextWorld`, `gameLoop`, `save/load`)을 인덱싱한다.
2. 각 함수가 읽고/쓰는 `game.*` 상태 키를 추적한다.
3. 상태 변경이 UI 반영 함수(`update*`)로 어떻게 연결되는지 확인한다.
4. 오프라인/챌린지/우주(유니버스) 등 모드별 분기점을 별도로 표시한다.

### 우선 분석 파일
- `main.js` (최우선)
- `config.js`
- `updates.js`

### 추출할 정보
- 시스템 분류표(예시)
  - 저장/불러오기
  - 전투 계산
  - 월드/맵 이동
  - 자원 생산/소모
  - 포탈/리셋
  - 오프라인 진행
  - 튜토리얼/이벤트
- 시스템별
  - 진입 함수
  - 핵심 상태 키
  - 출력(상태 변경/메시지/UI)

### 문서화 방식
- `CORE_SYSTEMS.md` 표 형식
  - `System | Entry points | State read/write | UI hooks | Notes`

---

## 3) 게임 루프 이해 방법

### Step-by-step
1. `gameLoop` 시그니처와 호출 주기를 먼저 파악한다.
2. 한 틱(tick) 안에서 수행되는 작업 순서를 실제 코드 기준으로 번호화한다.
3. 루프 내부에서 전투/자원/진행/UI가 어떤 순서로 결합되는지 정리한다.
4. `offlineProgress` 관련 흐름을 별도 시퀀스로 분리한다.
5. 루프 지연/보정(`setTimeout`, make-up tick) 처리 방식을 분석한다.

### 우선 분석 파일
- `main.js` (루프, 전투, 오프라인)
- `config.js` (루프에 영향 주는 옵션 기본값)

### 추출할 정보
- 메인 루프 단계도
  1) 시간 계산
  2) 시뮬레이션 업데이트
  3) 전투 처리
  4) 스테이지/월드 진행 판단
  5) UI 업데이트
  6) 다음 루프 스케줄링
- 보정 로직
  - 프레임 드리프트 처리
  - 오프라인 시간 재생산 방식

### 문서화 방식
- `GAME_LOOP_FLOW.md`
  - 루프 시퀀스 다이어그램
  - 동기/비동기 경계
  - 틱당 책임 목록

---

## 4) UI 로직 분석 방법

### Step-by-step
1. 화면 영역을 먼저 분해(상단 자원/중앙 전투/하단 패널/모달)한다.
2. 각 영역을 갱신하는 `update*` 함수 집합을 매핑한다.
3. 입력 이벤트(버튼 클릭, 토글, 슬라이더)가 어떤 명령 함수로 가는지 역추적한다.
4. 상태 변화 후 어떤 최소 UI만 갱신하는지, 전체 갱신이 필요한지 분류한다.
5. 접근성 관련 분기(`usingScreenReader`)가 UI 렌더링에 주는 영향도 기록한다.

### 우선 분석 파일
- `index.html`
- `main.js`
- `css/style.css`, `css/dark.css` (필요 시)

### 추출할 정보
- DOM ID ↔ 업데이트 함수 매핑
- 이벤트 입력 ↔ 상태 변경 함수 매핑
- 메시지 시스템/툴팁/알림 렌더 경로
- UI 성능 관점 병목(반복 DOM 접근, 대량 innerHTML 교체)

### 문서화 방식
- `UI_LOGIC_MAP.md`
  - `UI Section | DOM IDs | Update funcs | User actions | State dependencies`

---

## 5) 게임 성장(Progression) 시스템 분석 방법

### Step-by-step
1. 성장 축을 분리한다: 월드 진행, 업그레이드, 장비, 건물, 직업, 맵, 챌린지, 포탈.
2. 각 축의 해금 조건과 비용 공식을 찾는다.
3. 리셋(포탈) 시 유지/초기화되는 상태를 표로 만든다.
4. 성장 속도에 영향을 주는 곱연산 버프/스택 시스템을 추적한다.
5. 후반 콘텐츠(예: Spire) 연결 지점을 포함해 전체 성장 루프를 그린다.

### 우선 분석 파일
- `config.js` (정적 정의/기본값)
- `main.js` (해금/구매/진행 로직)
- `playerSpire.js` (특수 진행 루프)
- `updates.js` (성장 시스템 변경 이력 단서)

### 추출할 정보
- 해금 트리와 선행 조건
- 비용/효율 계산 지점
- 리셋 전후 상태 변화
- 성장 밸런스에 영향 큰 핵심 변수 목록

### 문서화 방식
- `PROGRESSION_MODEL.md`
  - 해금 트리
  - 리셋/유지 매트릭스
  - 성장 레버(밸런싱 포인트) 목록

---

## 실전 분석 순서 (권장 워크플로우)

1. **1차 스캔(반나절)**
   - 파일별 역할 라벨링
   - 핵심 함수 인덱스 생성
2. **2차 심층(1~2일)**
   - 게임 루프/전투/진행/저장 로직 정밀 추적
3. **UI & 성장 분석(1일)**
   - DOM-함수-상태 매핑
   - 성장 루프/리셋 구조 정리
4. **문서 통합(반나절)**
   - 시스템 상호작용 다이어그램 완성
   - 미해결 이슈와 추가 조사 항목 정리

---

## 최종 산출물 체크리스트

- [ ] 파일 구조 및 책임 맵 완성
- [ ] 핵심 시스템 표 완성
- [ ] 게임 루프 시퀀스 문서 완성
- [ ] UI 로직 매핑 문서 완성
- [ ] 성장 시스템 모델 문서 완성
- [ ] 리팩터링 우선순위 제안(선택)

---

## 분석 시 유의사항

- Trimps는 전역 상태(`game`) 중심 구조이므로, 함수 단위보다 **상태 키 단위 추적**이 더 효과적이다.
- `updates.js`는 단순 보조 파일이 아니라 **아키텍처 진화 이력**이므로 반드시 읽어야 한다.
- UI 코드는 로직과 결합되어 있을 가능성이 높아, “렌더링 함수”와 “상태 변경 함수”를 명확히 분리해 기록해야 한다.
- 오프라인 진행(`offlineProgress`)은 일반 루프와 다른 시간 스케일을 가지므로 별도 다이어그램으로 관리한다.
